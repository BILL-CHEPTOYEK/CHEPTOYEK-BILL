import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// Use ReactDOM to render App
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
